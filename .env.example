# --- Public (exposed to browser, prefix with NEXT_PUBLIC_) ---
NEXT_PUBLIC_SITE_URL=https://aicostcalc.net
# NEXT_PUBLIC_GA_ID — no longer used. The GA4 measurement ID now lives in
# lib/analytics.ts (it is public data, not a secret). Safe to delete this
# variable from the Vercel dashboard; it is read by nothing.
NEXT_PUBLIC_PLAUSIBLE_DOMAIN=

# --- Server-only (no NEXT_PUBLIC_ prefix) ---
# Sentry, etc. — add when needed
